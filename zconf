#!/bin/sh

if [ -z "$WAYLAND_DISPLAY" ]
then
	dmenu="dmenu"
else
	dmenu="bemenu"
	wayland=1
fi

arg="$1"



if [ "$arg" = "--help" ] || [ "$arg" = "-h" ] 
then
	echo "-- help --"
	echo "$scriptname"
	if [ -n "$scriptdisc" ]
	then
		echo "-- discript --"
		echo "$scriptdisc"
	fi
	exit
fi
